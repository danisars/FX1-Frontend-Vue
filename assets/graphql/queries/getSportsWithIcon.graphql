query getSportsWithIcon {
  getSportsWithIcon  {
    count
    total
    next
    items {
      id
      name
      slug
      Icon {
        objectID
        objectType
        isSport
        PhotoURL
      }
    }
  }
}
